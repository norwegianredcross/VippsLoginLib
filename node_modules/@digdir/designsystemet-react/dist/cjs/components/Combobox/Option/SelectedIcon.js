'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var akselIcons = require('@navikt/aksel-icons');
var cl = require('clsx/lite');

const SelectedIcon = ({ multiple, selected }) => {
    return (jsxRuntime.jsx("div", { className: cl(multiple && 'ds-combobox__option__icon-wrapper', selected && 'ds-combobox__option__icon-wrapper--selected'), children: selected && (jsxRuntime.jsx(akselIcons.CheckmarkIcon, { className: 'ds-combobox__option__icon-wrapper__icon', "aria-hidden": true })) }));
};
SelectedIcon.displayName = 'SelectedIcon';

exports.SelectedIcon = SelectedIcon;
